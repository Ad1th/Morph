# Morph: Python dependencies
# Requires Python 3.11+
#
# Stack decision (PRD Section 23): Python, for the whole runtime. morph/profiler,
# morph/engine, and morph/runtime/adapters/proxy.py are built against this file.

# ---- CLI / TUI ----
typer>=0.12.0
rich>=13.7.0
textual>=1.0.0                   # `morph tui`: full-screen live experiment console

# ---- Local API (frontend <-> controller) ----
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9          # importing captured env profiles / error-log bundles via the API

# ---- Environment capture / profiling ----
psutil>=5.9.8                    # CPU/RAM/process/network telemetry, resource limits
py-cpuinfo>=9.0.0               # CPU model / arch / clock detail
distro>=1.9.0                    # Linux distribution identification
tzdata>=2024.1                   # timezone database (needed on Windows / minimal images)

# ---- Experiment engine (baseline vs treatment, threshold search, stats) ----
numpy>=1.26.0
scipy>=1.12.0                    # Fisher's exact test + two-proportion comparison (PRD Section 16)

# ---- HTTP client (n8n webhooks, Tin / cloud worker, health checks) ----
httpx>=0.27.0

# ---- Config / serialization ----
pyyaml>=6.0.1
python-dotenv>=1.0.0           # loads the nearest .env at startup (see .env.example)

# ---- Testing ----
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0            # bound hanging/flaky trial runs so CI can't stall

# ---- Lint / format (CI: runs on push, blocking) ----
ruff>=0.3.0

# ---- Optional: SGLang reporting layer (stretch, PRD Section 26) ----
# Kept commented so the core build has no LLM dependency. Uncomment only if the
# plain-English experiment-report feature is actually built.
# sglang>=0.2.0
