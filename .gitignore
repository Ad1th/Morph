# System files
.DS_Store
Thumbs.db


# Python
__pycache__/
*.py[cod]
*.class
*.so
.Python
env/
venv/
.venv/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Node / Frontend
node_modules/
dist/
.env.local
.env.*.local

# Morph local state: transient run output only
# NOTE: .morph/regressions/ is intentionally NOT ignored: regression artifacts are committed and replayed by CI.
.morph/runs/
.morph/cache/
*.log

# Graphify local tool state: regenerable extraction cache and change-detection
# manifest, not the tracked outputs (graph.html/json, GRAPH_REPORT.md).
graphify-out/cache/
graphify-out/manifest.json

# Env / secrets
.env

# Temporary / Draft Specs
Morph_Design_Spec.md
Morph_PRD.md

# local venv symlink (worktree) and runtime state
.venv
.morph/
